// Local preview placeholder — replace with real values before going live.
// (Submit will say "not configured" until owner/token are real.)
window.GITHUB_CONFIG = {
  owner:  "kadj-amoah",
  repo:   "bethel-kwadaso-announcements",
  branch: "main",
  path:   "data/latest.json",
  imagesDir: "data/images",
  token:  "PASTE_FINE_GRAINED_PAT_HERE",   // replaced once the PAT is created
};
